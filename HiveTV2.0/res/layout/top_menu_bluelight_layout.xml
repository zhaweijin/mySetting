<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/top_menu_bluelight"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingTop="15dip"
    android:clipChildren="false">

    <TextView
        android:id="@+id/navigation_tab_bluelight_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dip"
        android:background="@android:color/transparent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:singleLine="true"
        android:text="@string/navigation_tab_bluelight_text"
        android:textColor="@android:color/white"
        android:textSize="26.64sp" />

    <ImageView
        android:id="@+id/navigation_tab_bluelight_fade"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:background="@drawable/navigation_tab_fade_bg"
        android:contentDescription="@string/app_name"
        android:visibility="invisible" />

</RelativeLayout>