package com.hiveview.tv.view;import android.content.Context;import android.graphics.drawable.AnimationDrawable;import android.util.AttributeSet;import android.view.View;import android.widget.ImageView;import com.hiveview.tv.R;public class ProgressDialog extends ImageView {	public ProgressDialog(Context context, AttributeSet attrs, int defStyle) {		super(context, attrs, defStyle);		init();	}	public ProgressDialog(Context context, AttributeSet attrs) {		super(context, attrs);		init();	}	public ProgressDialog(Context context) {		super(context);		init();	}	@Override	public void setVisibility(final int visibility) {		if (visibility == View.INVISIBLE || visibility == View.GONE) {			postDelayed(new Runnable() {				@Override				public void run() {					setViewInvisble();				}				//start author:zhangpengzhan			},1);			//end 修改一个延时时间，qa要求出现画面立刻隐藏loading		} else {			super.setVisibility(visibility);		}	}	private void setViewInvisble() {		super.setVisibility(View.INVISIBLE);	}	private void init() {		this.setImageResource(R.drawable.animation_domy_loading);		final AnimationDrawable mAnimationDrawable = (AnimationDrawable) this.getDrawable();		mAnimationDrawable.start();	}}