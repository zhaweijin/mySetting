<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center" />

    <RelativeLayout
        android:id="@+id/appDownloadIcon_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dip"
         android:visibility="gone"
        android:background="@drawable/as_progressbar_bg" >

        <ProgressBar
            android:id="@+id/appDownloadIcon"
            style="@style/as_progress_style_app"
            android:layout_margin="3dip"
            android:visibility="gone" />
    </RelativeLayout>

    <com.hiveview.tv.view.InstallProgressView
        android:id="@+id/appDownloadSetUpIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dip"
        android:background="@drawable/as_progressbar_bg"
        android:padding="3dip"
        android:visibility="gone" />

</RelativeLayout>